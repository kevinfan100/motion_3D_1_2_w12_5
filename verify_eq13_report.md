# Eq. 13 Measured Motion Gain — 驗證報告

## 1. 驗證目標

論文 Eq. 13 從追蹤誤差的隨機分量變異數反推運動增益：

```
Eq. 12:  sig2_dxr = (2 + 1/(1 - lc^2)) × 4×kb×T×a_x
Eq. 13:  a_xm    = sig2_dxr / { 4×kb×T × (2 + 1/(1 - lc^2)) }
```

核心邏輯：sig2_dxr 隨 lc 增大而增大，但 Eq. 13 的分母同步補償，
使 a_xm 在所有 lc 下都還原為常數 `a_x = Ts / gamma_N`。

---

## 2. 模擬設定

### 2.1 物理參數

| 參數 | 值 | 說明 |
|------|-----|------|
| Ts | 1/1600 s | 取樣時間 |
| T | 310.15 K (37 C) | 溶液溫度 |
| kb | 1.3806503e-23 J/K | 波茲曼常數 |
| R | 2.25 um | 探針半徑 |
| eta | 0.001 Pa·s | 水的動態黏度 |
| gamma_N | 0.0425 pN·s/um | 名義 Stokes 阻力 |
| sig2_nx | 0 | 量測雜訊（忽略） |
| a_x_true | Ts/gamma_N = 0.01471 um/pN | 理論運動增益（驗證目標） |

### 2.2 簡化條件與 1D 模擬說明

| 簡化 | 目的 |
|------|------|
| gamma = gamma_N（常數，無牆壁效應） | a_x 為已知常數，可明確檢驗 |
| 探針靜止（固定目標位置） | 確定性分量 = 0，sig2_dxr = Var(dzm) |
| sig2_nx = 0 | 排除量測雜訊 |
| **只模擬單一軸（1D）** | 見下方說明 |

**為什麼只做 1D 而非 3D：**

實際系統是三維的（x, y, z），但在本驗證的簡化條件下（gamma = gamma_N，無牆壁）：
- 三軸完全解耦，各軸動態獨立且形式相同
- 每軸都是 `pos[k+1] = pos[k] + a_x × (fd[k] + fT[k])`
- Eq. 13 獨立適用於每一軸

因此驗證一軸即可代表所有軸。實際系統中若有牆壁效應，
平行與垂直方向的 gamma 修正不同（c_par vs c_perp），
但 Eq. 13 的形式不變，只是各軸的 a_x 值不同。

### 2.3 模擬參數

| 參數 | 值 |
|------|-----|
| 測試的 lc 值 | [0.3, 0.5, 0.7, 0.9] |
| 模擬步數 | 80000 步（50 秒） |
| 穩態區間 | 後 40000 步（後 25 秒） |
| 隨機數種子 | rng(42 + idx)，可重現 |

### 2.4 熱力噪聲

```
gamma_N_SI = 6×pi×eta×R = 4.2412e-8 N·s/m
sig2_fT    = 4×kb×T×gamma_N_SI / Ts = 1.163e-24 N^2 = 1.163 pN^2
sigma_fT   = 1.078 pN
```

---

## 3. 控制器設計

### 3.1 Eq. 13 對控制器的要求

Eq. 12 的推導基於閉迴路誤差動態 `e[k+1] = lc × e[k] + noise`，
因此控制器必須滿足：

1. **閉迴路極點 = lc**
2. **保留兩步量測延遲的效應**（公式中 "2" 的來源）

### 3.2 為什麼不能用 naive 控制器

原始計畫的控制器：`fd[k] = (1/a_x) × (1-lc) × dzm[k]`

直接用延遲了兩步的量測計算控制力，產生三階閉迴路：

```
e[k+1] = e[k] - (1-lc) × e[k-2] + noise
特徵方程：z^3 - z^2 + (1-lc) = 0
```

| lc | 最大極點模 | 穩定？ | 極點 = lc？ |
|----|-----------|--------|------------|
| 0.3 | **1.037** | **不穩定** | 否 |
| 0.5 | 0.941 | 穩定 | 否（偏離） |
| 0.7 | 0.813 | 穩定 | 否（偏離） |
| 0.9 | 0.867 | 穩定 | 否（偏離） |

極點 ≠ lc → Eq. 12 的推導前提不成立 → Eq. 13 無法還原 a_x。

### 3.3 預測器控制器（正確的簡化）

Simulink 模型使用 7 狀態 Kalman 濾波器，其中前三個狀態
（dz1 → dz2 → dz3）構成延遲補償鏈。在簡化條件下
（已知 gamma、無擾動、靜止探針），等效為兩步預測器：

**推導：**

從 k-2 到 k 的受控體動態：
```
z[k] = z[k-2] + a_x×(fd[k-2] + fd[k-1]) + a_x×(fT[k-2] + fT[k-1])
       |已知：量測|  |已知：控制歷史|        |未知：熱力噪聲|
```

預測當前位置（忽略未知噪聲）：
```
z_hat[k] = z[k-2] + a_x × (fd[k-2] + fd[k-1])
```

預測誤差：
```
e_hat[k] = dzm[k] - a_x × (fd[k-2] + fd[k-1])
```

控制律：
```
fd[k] = (1/a_x) × (1-lc) × e_hat[k]
```

預測精度：
```
e_hat[k] - e[k] = a_x × (fT[k-2] + fT[k-1])    ← 無偏差，但有隨機誤差
```

---

## 4. Step 1：控制器分析

**目的：** 在驗證 Eq. 13 之前，先確認控制器確實產生了公式推導所假設的
閉迴路動態（極點 = lc）。如果這個前提不成立，後續驗證就沒有意義。

### 4.1 閉迴路狀態空間

把預測器控制器代入受控體方程後，閉迴路動態為：

```
e[k+1] = lc × e[k] - a_x×fT[k] - (1-lc)×a_x×(fT[k-1] + fT[k-2])
         ↑ 極點項      ↑ 當步噪聲    ↑ 延遲期間無法補償的噪聲
```

e[k+1] 不只取決於 e[k]，還取決於過去兩步的噪聲歷史。
因此把狀態擴充為 `x[k] = [e[k]; fT[k-1]; fT[k-2]]`，寫成矩陣形式：

```
x[k+1] = A × x[k] + B × fT[k]

A = [ lc,  -(1-lc)×a_x,  -(1-lc)×a_x ]     B = [ -a_x ]
    [  0,       0,              0       ]         [   1   ]
    [  0,       1,              0       ]         [   0   ]
```

- 第 1 行：閉迴路誤差動態
- 第 2、3 行：噪聲記憶（把 fT 歷史往後搬一格）

### 4.2 極點驗證

A 的特徵值決定系統的穩定性與收斂速度。
因為 A 是下三角結構，特徵值直接就是對角線元素 = **[lc, 0, 0]**。

| lc | 系統極點 | 誤差 | 意義 |
|----|---------|------|------|
| 0.3 | 0.3 | 0% | 每步誤差衰減為 0.3 倍 |
| 0.5 | 0.5 | 0% | 每步誤差衰減為 0.5 倍 |
| 0.7 | 0.7 | 0% | 每步誤差衰減為 0.7 倍 |
| 0.9 | 0.9 | 0% | 每步誤差衰減為 0.9 倍 |

**極點 = lc，控制器設計正確。**

### 4.3 Lyapunov 方程驗證

穩態時，誤差的衰減量 = 噪聲的注入量，變異數不再變化。
離散 Lyapunov 方程直接解出這個平衡點：

```
P = A × P × A' + B × sig2_fT × B'
↑       ↑              ↑
穩態    一步後的         一步內加入的
共變異  衰減效果         噪聲能量
```

MATLAB 用 `dlyap(A, Q)` 解出 P，其中 P(1,1) = Var(e) 就是穩態追蹤誤差變異數。

| lc | Var(Lyapunov) (um^2) | Var(Eq.12) (um^2) | 誤差 |
|----|---------------------|-------------------|------|
| 0.3 | 7.790e-04 | 7.806e-04 | **0.21%** |
| 0.5 | 8.379e-04 | 8.396e-04 | **0.21%** |
| 0.7 | 9.956e-04 | 9.977e-04 | **0.21%** |
| 0.9 | 1.826e-03 | 1.830e-03 | **0.21%** |

**Eq. 12 在理論層面精確成立（誤差 0.21%），不需要任何模擬即可確認。**

### 4.4 噪聲結構：有色噪聲

閉迴路的驅動噪聲 `w[k] = -a_x×fT[k] - (1-lc)×a_x×(fT[k-1] + fT[k-2])`
包含三個時步的 fT，使得 w[k] 和 w[k-1] 共享了 fT[k-1] 和 fT[k-2]，
產生正相關（有色噪聲）。

這導致追蹤誤差的自相關 R(1)/R(0) 高於純 AR(1) 白噪聲的預期值 lc：

| lc | R(1)/R(0) 實際 | 白噪聲 AR(1) 預期值 | 差異 |
|----|---------------|-------------------|------|
| 0.3 | 0.752 | 0.300 | +150.4% |
| 0.5 | 0.801 | 0.500 | +60.3% |
| 0.7 | 0.851 | 0.700 | +21.6% |
| 0.9 | 0.928 | 0.900 | +3.1% |

lc 越小 → (1-lc) 越大 → 噪聲中 fT[k-1], fT[k-2] 權重越高 → 相關性越強。

**這不影響 Eq. 12/13 的正確性。** Eq. 12 的公式 `(2 + 1/(1-lc^2))` 本來就是
為這個有色噪聲系統推導的：
- `1/(1-lc^2)`：極點 lc 造成的變異數放大
- `2`：兩步延遲噪聲的額外貢獻（有色噪聲的來源）

Lyapunov 方程的精確驗證（0.21%）證實了公式已正確考慮這個效應。

### 4.5 Step 1 小結

| 分析項目 | 結果 | 意義 |
|---------|------|------|
| 極點 = lc | 0% 誤差 | 控制器設計正確 |
| Lyapunov Var(e) vs Eq. 12 | 0.21% 誤差 | 公式在理論層面精確成立 |
| 有色噪聲自相關 | R(1)/R(0) > lc | 不影響公式，已被 (2+...) 補償 |

**結論：控制器確實產生了 Eq. 12 預期的閉迴路動態，Eq. 13 的前提條件成立。**

---

## 5. Step 2：Eq. 12 驗證 — sig2_dxr 隨 lc 變化

模擬的 Var(dzm)（穩態區間直接計算）與 Eq. 12 理論值比較：

| lc | C(lc) = 2+1/(1-lc^2) | sig2 理論 (um^2) | sig2 模擬 (um^2) | 誤差 |
|----|----------------------|-----------------|-----------------|------|
| 0.3 | 3.099 | 7.806e-04 | 7.683e-04 | **1.57%** |
| 0.5 | 3.333 | 8.396e-04 | 8.484e-04 | **1.04%** |
| 0.7 | 3.961 | 9.977e-04 | 1.011e-03 | **1.37%** |
| 0.9 | 7.263 | 1.830e-03 | 1.854e-03 | **1.33%** |

sig2_dxr 從 lc=0.3 的 7.68e-04 增長到 lc=0.9 的 1.85e-03（增長 2.4 倍），
與 C(lc) 從 3.099 增長到 7.263 的趨勢一致。所有誤差 < 2%。

---

## 6. Step 3：Eq. 13 驗證 — a_xm 還原 a_x

將模擬的 sig2_dxr 代入 Eq. 13：

```
a_xm = (sig2_sim × 1e-12) / { 4×kb×T × (2 + 1/(1-lc^2)) } × 1e-6   [um/pN]
```

| lc | a_x_true (um/pN) | a_xm (um/pN) | 誤差 |
|----|-----------------|-------------|------|
| 0.3 | 0.01471 | 0.01448 | **1.57%** |
| 0.5 | 0.01471 | 0.01486 | **1.04%** |
| 0.7 | 0.01471 | 0.01491 | **1.37%** |
| 0.9 | 0.01471 | 0.01490 | **1.33%** |

**PASS：所有 lc 的 a_xm 都收斂到 a_x_true，最大誤差 1.57%。**

---

## 7. 關鍵發現總結

### 7.1 控制器必須補償兩步延遲

| 控制器 | 極點 = lc？ | Eq.13 誤差 |
|--------|-----------|-----------|
| naive: `fd = (1/a_x)×(1-lc)×dzm` | 否（三階極點偏離） | lc=0.3 不穩定，其餘 > 75% |
| 預測器: `e_hat = dzm - a_x×(fd[k-2]+fd[k-1])` | 是（誤差 0%） | < 2% |
| Simulink Kalman 濾波器 | 是（dz1→dz2→dz3 狀態鏈） | — |

預測器控制器是 Kalman 濾波器在簡化條件下的解析等效。

### 7.2 Eq. 13 成立的條件

1. **閉迴路極點 = lc**（控制器增益為 `(1-lc)/a_x`，且有延遲補償）
2. **兩步量測延遲存在**（對應公式中的 "2"）
3. **受控體模型**：`pos[k+1] = pos[k] + a_x × (fd[k] + fT[k])`

### 7.3 Eq. 13 的物理意義

利用漲落耗散定理：微球的熱力抖動（sig2_dxr）與其可動性（a_x）成正比。
量測抖動大小，扣除控制器和延遲的放大效應，即可即時反推局部阻力係數。

---

## 8. 檔案清單

| 檔案 | 用途 |
|------|------|
| `verify_eq13.m` | 核心驗證腳本（含預測器控制器 + 直接變異數法） |
| `verify_eq13_report.md` | 本報告 |
| `path_control.m` | 參考 — Simulink 模型參數與繪圖 |
| `motion_3D_1_2_w12_5.slx` | 參考 — 完整 Simulink 模型（含 Kalman 濾波器） |
