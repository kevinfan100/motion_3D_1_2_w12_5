# Eq. 13 Measured Motion Gain — 驗證報告

## 1. 驗證目標

論文 Eq. 13 從追蹤誤差的隨機分量變異數反推運動增益 a_x = Ts/gamma。
在簡化條件下驗證：對不同 lc 值，Eq. 13 都能正確還原已知的 a_x。

---

## 2. 論文公式鏈：Eq. 7 → Eq. 11 → Eq. 12 → Eq. 13

### Eq. 7-8：控制目標與殘餘擾動

論文的控制目標是讓追蹤誤差以 lc 的速率衰減：
```
dx[k+1] = lc × dx[k] - e[k]                                    ... (Eq. 7)
e[k] = ax×fT[k] - (1-lc)×edx[k] + ea[k]×fdx[k] + exD[k]      ... (Eq. 8)
```
如果估測器是無偏的（edx, ea, exD 均值為零），殘餘擾動 e[k] 就是純隨機的，
追蹤誤差也是零均值隨機誤差。

### Eq. 9-10：IIR 濾波器量測 sig2_dxr

```
dxm_bar[k+1] = avar × dxm[k] + (1-avar) × dxm_bar[k]     ... (Eq. 9) 確定性分量
dxr[k] = dxm[k] - dxm_bar[k]                                隨機分量
sig2_dxr = E[dxr^2] - E[dxr]^2                             ... (Eq. 10) 變異數
```

### Eq. 11：sig2_dxr 的理論值（無量測雜訊）

```
sig2_dxr = (2 + 1/(1-lc^2)) × 4×kb×T×ax                   ... (Eq. 11)
```
其中 `(2 + 1/(1-lc^2))` 的兩個部分：
- `1/(1-lc^2)`：閉迴路 AR(1) 極點 lc 造成的變異數放大
- `2`：兩步量測延遲期間，估測器無法觀測的熱力噪聲（每步貢獻 1 個單位）

### Eq. 12：加入量測雜訊

```
sig2_dxr = (2 + 1/(1-lc^2)) × 4×kb×T×ax + (2/(1+lc)) × sig2_nx   ... (Eq. 12)
```

### Eq. 13：反解 ax — 從抖動推阻力

```
axm = { sig2_dxr - (2/(1+lc))×sig2_nx } / { 4×kb×T × (2 + 1/(1-lc^2)) }  ... (Eq. 13)
```
物理意義：分子扣掉量測雜訊，分母扣掉控制器和延遲的放大效應，剩下的就只跟 ax 有關。

本驗證中 sig2_nx = 0，Eq. 13 簡化為：
```
axm = sig2_dxr / { 4×kb×T × (2 + 1/(1-lc^2)) }
```

---

## 3. 模擬設定

### 3.1 物理參數

| 參數 | 值 | 說明 |
|------|-----|------|
| Ts | 1/1600 s | 取樣時間 |
| T | 310.15 K (37 C) | 溶液溫度 |
| kb | 1.3806503e-23 J/K | 波茲曼常數 |
| R | 2.25 um | 探針半徑 |
| eta | 0.001 Pa·s | 水的動態黏度 |
| gamma_N | 0.0425 pN·s/um | 名義 Stokes 阻力 |
| sig2_nx | 0 | 量測雜訊（忽略） |
| a_x_true | Ts/gamma_N = 0.01471 um/pN | 理論運動增益（驗證目標） |

### 3.2 簡化條件

| 簡化 | 目的 |
|------|------|
| gamma = gamma_N（常數，無牆壁效應） | a_x 為已知常數，可明確檢驗 |
| 探針靜止（固定目標位置） | 確定性分量 = 0，sig2_dxr = Var(dzm) |
| sig2_nx = 0 | 排除量測雜訊，簡化為 Eq. 11 |
| 只模擬單一軸（1D） | 三軸解耦且形式相同，驗證一軸即代表所有軸 |

### 3.3 模擬參數

| 參數 | 值 |
|------|-----|
| 測試的 lc 值 | [0.3, 0.5, 0.7, 0.9] |
| 模擬步數 | 80000 步（50 秒） |
| 穩態區間 | 後 40000 步 |
| 隨機數種子 | rng(42 + idx)，可重現 |

### 3.4 受控體模型與熱力噪聲

```
受控體：z[k+1] = z[k] + a_x × (fd[k] + fT[k])          ... (Eq. 5, 1D)
量測：  dzm[k] = z_d - z[k-2]                             (兩步延遲)

熱力噪聲：fT ~ N(0, sig2_fT)
  gamma_N_SI = 6×pi×eta×R = 4.2412e-8 N·s/m
  sig2_fT = 4×kb×T×gamma_N_SI / Ts = 1.163 pN^2
  sigma_fT = 1.078 pN
```

---

## 4. 控制器設計

### 4.1 論文 Eq. 6（理想控制律）

```
fd[k] = (1/a_hat) × ( xd[k+1]-xd[k] + (1-lc)×dx_hat[k] - xD_hat[k] )   ... (Eq. 6)
         ↑ 估測的 a_x     ↑ 軌跡變化       ↑ 估測的誤差      ↑ 估測的擾動
```

其中 dx_hat[k] 由 Kalman 濾波器提供（7 狀態，含延遲補償鏈 dz1→dz2→dz3）。

在簡化條件下（已知 gamma、無擾動、靜止探針），Eq. 6 退化為：
```
fd[k] = (1/a_x) × (1-lc) × dx_hat[k]
```

### 4.2 為什麼不能直接用量測值 dzm[k]

量測 dzm[k] = z_d - z[k-2] 延遲了兩步。直接用它做控制：
```
fd[k] = (1/a_x) × (1-lc) × dzm[k]     ← naive 控制器
```

產生三階閉迴路 `e[k+1] = e[k] - (1-lc)×e[k-2] + noise`：

| lc | 最大極點模 | 穩定？ | 極點 = lc？ |
|----|-----------|--------|------------|
| 0.3 | **1.037** | **不穩定** | 否 |
| 0.5 | 0.941 | 穩定 | 否 |
| 0.7 | 0.813 | 穩定 | 否 |
| 0.9 | 0.867 | 穩定 | 否 |

極點 ≠ lc → Eq. 11 前提不成立 → Eq. 13 無法還原 a_x。

### 4.3 Smith Predictor 延遲補償

Kalman 濾波器在簡化條件下退化為 Smith Predictor（確定性估測器），
核心思想：用已知的控制歷史把延遲量測「推回」當前時刻。

**Step A — 受控體兩步展開：**
```
z[k] = z[k-2] + a_x×(fd[k-2] + fd[k-1]) + a_x×(fT[k-2] + fT[k-1])
                 \_________已知__________/   \________未知________/
```

**Step B — 預測當前誤差：**
```
e_hat[k] = dzm[k] - a_x×(fd[k-2] + fd[k-1])
```

**Step C — 預測精度：**
```
e_hat[k] = e[k] + a_x×(fT[k-2] + fT[k-1])
                   ↑ 無偏差，但兩步延遲噪聲無法消除
```

**Step D — 控制律：**
```
fd[k] = (1/a_x) × (1-lc) × e_hat[k]
```

**Step E — 閉迴路動態（代入受控體方程）：**
```
e[k+1] = e[k] - (1-lc)×{e[k] + a_x×(fT[k-2]+fT[k-1])} - a_x×fT[k]
       = lc×e[k] - a_x×fT[k] - (1-lc)×a_x×(fT[k-1] + fT[k-2])
```

最終形式：
```
e[k+1] = lc × e[k] + w[k]

w[k] = -a_x × { fT[k] + (1-lc)×fT[k-1] + (1-lc)×fT[k-2] }
```
- 極點 = lc（滿足 Eq. 7 的控制目標）
- 噪聲 w[k] 是有色的（包含三個時步的 fT）

### 4.4 Smith Predictor 與 Kalman 濾波器的關係

| | Smith Predictor（本驗證） | Kalman 濾波器（Simulink） |
|------|------|------|
| 類型 | 確定性估測器 | 最優隨機估測器 |
| 輸入 | 系統模型 + 控制歷史 | 系統模型 + 控制歷史 + 噪聲統計 |
| 適用 | 已知 gamma、無擾動 | 通用（未知 gamma、有擾動） |

在簡化條件下兩者等價。

---

## 5. 驗證結果

### 5.1 Step 1：控制器分析（Eq. 11 前提驗證）

**目的：** 確認控制器確實產生極點 = lc 的閉迴路動態。

擴充狀態空間 `x = [e[k]; fT[k-1]; fT[k-2]]`：
```
A = [ lc,  -(1-lc)×a_x,  -(1-lc)×a_x ]     B = [ -a_x ]
    [  0,       0,              0       ]         [   1   ]
    [  0,       1,              0       ]         [   0   ]
```

**(a) 極點驗證** — A 的特徵值 = [lc, 0, 0]，極點精確等於 lc（0% 誤差）。

**(b) Lyapunov 方程驗證** — 解 `P = A×P×A' + B×sig2_fT×B'`，
P(1,1) = Var(e) 直接給出穩態變異數（不需模擬）：

| lc | Var(Lyapunov) (um^2) | Var(Eq.11) (um^2) | 誤差 |
|----|---------------------|-------------------|------|
| 0.3 | 7.790e-04 | 7.806e-04 | **0.21%** |
| 0.5 | 8.379e-04 | 8.396e-04 | **0.21%** |
| 0.7 | 9.956e-04 | 9.977e-04 | **0.21%** |
| 0.9 | 1.826e-03 | 1.830e-03 | **0.21%** |

**(c) 有色噪聲確認** — w[k] 包含 fT[k], fT[k-1], fT[k-2]，
導致自相關 R(1)/R(0) > lc（如 lc=0.7 時 0.851 > 0.7）。
Eq. 11 的 `(2 + 1/(1-lc^2))` 已包含此效應，Lyapunov 驗證精度 0.21%。

**Step 1 結論：控制器設計正確，Eq. 11 前提成立。**

### 5.2 Step 2：Eq. 11 驗證 — sig2_dxr 隨 lc 變化

模擬 80000 步，穩態區間直接計算 Var(dzm)，與 Eq. 11 理論值比較：

| lc | C(lc) = 2+1/(1-lc^2) | sig2 理論 (um^2) | sig2 模擬 (um^2) | 誤差 |
|----|----------------------|-----------------|-----------------|------|
| 0.3 | 3.099 | 7.806e-04 | 7.683e-04 | **1.57%** |
| 0.5 | 3.333 | 8.396e-04 | 8.484e-04 | **1.04%** |
| 0.7 | 3.961 | 9.977e-04 | 1.011e-03 | **1.37%** |
| 0.9 | 7.263 | 1.830e-03 | 1.854e-03 | **1.33%** |

sig2_dxr 從 lc=0.3 的 7.68e-04 增長到 lc=0.9 的 1.85e-03（增長 2.4 倍），
與 C(lc) 從 3.099 增長到 7.263 的趨勢一致。**所有誤差 < 2%。**

### 5.3 Step 3：Eq. 13 驗證 — a_xm 還原 a_x

將模擬 sig2_dxr 代入 Eq. 13（sig2_nx = 0）：

| lc | a_x_true (um/pN) | a_xm (um/pN) | 誤差 |
|----|-----------------|-------------|------|
| 0.3 | 0.01471 | 0.01448 | **1.57%** |
| 0.5 | 0.01471 | 0.01486 | **1.04%** |
| 0.7 | 0.01471 | 0.01491 | **1.37%** |
| 0.9 | 0.01471 | 0.01490 | **1.33%** |

**PASS：所有 lc 的 a_xm 收斂到 a_x_true，最大誤差 1.57%。**

---

## 6. Eq. 11 中 (2 + 1/(1-lc^2)) 的物理來源

```
物理現實                         對 sig2_dxr 的貢獻                    公式項
─────────                       ────────────────                     ──────
磁珠受熱力噪聲 fT[k] 推動        每步注入 4×kb×T×ax 的變異數

  ↓ 經過閉迴路 (極點 = lc)
  控制器每步只修正 (1-lc)        AR(1) 放大 1/(1-lc^2) 倍             ← 1/(1-lc^2)
  殘餘誤差以 lc 倍累積

  ↓ 兩步量測延遲
  fT[k-2], fT[k-1] 在延遲       每步額外貢獻 1 個噪聲單位             ← "2"
  期間無法被觀測或補償            兩步 = 2 個單位

合計                            (2 + 1/(1-lc^2)) × 4×kb×T×ax         ← Eq. 11
```

---

## 7. 3 狀態估測器對照實驗

### 7.1 動機

論文 Eq. 6 的控制律使用 Kalman 濾波器（Eq. 16）提供 dx_hat[k]。
在已知 gamma 的條件下，嘗試用簡化的 3 狀態估測器（dx1→dx2→dx3 狀態鏈
+ innovation 修正）取代 Smith Predictor，測試是否也能驗證 Eq. 13。

### 7.2 估測器建構

**3 個狀態：**
```
dx1_hat ≈ dx[k-2]   （量測可校正）
dx2_hat ≈ dx[k-1]   （模型推算）
dx3_hat ≈ dx[k]     （模型推算，控制律使用）
```

**估測器更新方程：**
```
innovation = dzm[k] - dx1_hat

dx1_hat_new = dx2_hat       + L1 × innovation
dx2_hat_new = dx3_hat       + L2 × innovation
dx3_hat_new = lc × dx3_hat  + L3 × innovation

fd[k] = (1/a_x) × (1-lc) × dx3_hat_new
```

**L 增益（極點配置法）：**
估測誤差動態矩陣 Fe = [0,1,0; 0,0,1; 0,0,1]，
指定所有極點 = lambda_e，解出：
```
L1 = 1 - 3×le
L2 = 1 - 3×le + 3×le^2
L3 = (1 - le)^3
```

### 7.3 閉迴路極點分析

估測器引入 3 個額外狀態（edx1, edx2, edx3 = 估測誤差），
整體系統為 6 階，極點分為兩組：

```
Smith Predictor:     [lc, 0, 0]              ← 3 個極點
3 狀態估測器:        [lc, 0, 0, le, le, le]  ← 6 個極點
                      ↑ plant     ↑ estimator error
```

**兩者的 plant 極點都在 lc，但估測器多了 3 個估測誤差極點。**

6×6 系統矩陣：
```
         dx1  dx2  dx3  edx1  edx2  edx3
dx1  [    0    1    0    0     0     0   ]
dx2  [    0    0    1    0     0     0   ]
dx3  [    0    0   lc    0     0   1-lc  ]  ← edx3 回饋進 dx3
edx1 [    0    0    0   -L1    1     0   ]
edx2 [    0    0    0   -L2    0     1   ]
edx3 [    0    0    0   -L3    0     1   ]
```

dx3 的動態包含 `(1-lc)×edx3` 耦合項：控制律用 dx3_hat（不是真實 dx3），
差異 edx3 = dx3 - dx3_hat 透過控制力回饋，構成額外的噪聲路徑。

### 7.4 估測器驗證

**(a) 開迴路測試（不接控制迴路）：**
生成已知 AR(1) 信號 dx3[k]，取 dx1[k] = dx3[k-2] 作為量測，
餵入估測器，檢查 dx3_hat 能否還原 dx3。

| 指標 | 結果 |
|------|------|
| corr(dx3_hat, dx3_true) | 0.473 |
| 理論最佳（lc^2） | 0.490 |
| 達到理論最佳的 | **96.4%** → 估測器正確 |
| Innovation 均值 | ≈ 0 → 無偏 |

**(b) 閉迴路測試：**

| 指標 | 結果 |
|------|------|
| corr(dx3_hat, e_true) | 0.746 |
| Innovation 均值 | ≈ 0 → 無偏 |
| Innovation 自相關 | 0.700（遠離 0 → 非最優估測） |
| 系統穩定 | 是 |

### 7.5 Eq. 13 結果與 lambda_e 影響（Lyapunov 精確分析，lc = 0.7）

| lambda_e | Var(edx3) | Var(dx3) | Var/Eq.11 | Eq.13 誤差 |
|----------|-----------|----------|-----------|-----------|
| 0（deadbeat） | 7.54e-04 | 1.25e-03 | 1.250 | **25.0%** |
| 0.3 | 9.01e-04 | 1.51e-03 | 1.513 | **51.3%** |
| 0.5 | 1.18e-03 | 1.84e-03 | 1.849 | **84.9%** |
| 0.7 | 1.86e-03 | 2.59e-03 | 2.593 | **159.3%** |
| **Smith Predictor** | **—** | **9.96e-04** | **0.998** | **0.2%** |

lambda_e 越大 → 估測器越慢 → edx3 越大 → 回饋噪聲越多 → Var(dx3) 偏離越大。
即使 lambda_e = 0（最快），Var(dx3) 仍比 Eq. 11 理論多 25%。

### 7.6 根本原因：噪聲路徑差異

```
Smith Predictor:
  fT → dx3                           （1 條路徑）
  Var(dx3) = Eq.11 理論值 ✓

3 狀態估測器:
  fT → dx3                           （路徑 1：直接）
  fT → edx3 → (1-lc)×edx3 → dx3     （路徑 2：經估測誤差回饋）
  Var(dx3) > Eq.11 理論值 ✗
```

Smith Predictor 用代數公式 `e_hat = dzm - a_x×(fd[k-2]+fd[k-1])` 直接消除
兩步的控制效果，不經過狀態遞迴。估測器必須透過 dx1→dx2→dx3 狀態鏈傳遞
資訊，每步累積估測誤差。

### 7.7 結論

- **閉迴路極點 = lc 是必要條件但不是充分條件。**
  Eq. 13 還需要噪聲結構匹配 Eq. 17 推導的形式。
- 3 狀態估測器的極點在 lc，但額外的估測誤差噪聲路徑改變了 Var(dx3)。
- **Eq. 13 綁定於 Eq. 17（= Smith Predictor）的控制律結構。**
- 在已知 gamma 時不需要估測器；在未知 gamma 時，完整 Kalman 濾波器
  以不同方式處理（同時估測 ax 和 dx，axm 作為量測輸入）。

---

## 8. 關鍵結論

### 8.1 Eq. 13 成立的條件（精確版）

1. **閉迴路極點 = lc** — 必要但不充分
2. **噪聲結構匹配 Eq. 17** — 控制律必須是 Smith Predictor 形式
3. **兩步量測延遲** — 對應公式中的 "2"
4. **受控體模型** — `pos[k+1] = pos[k] + a_x × (fd[k] + fT[k])`

### 8.2 三種控制器的完整比較

| 控制器 | 極點 = lc？ | 噪聲結構匹配？ | Eq.13 誤差 |
|--------|-----------|--------------|-----------|
| naive（不補償延遲） | 否 | 否 | lc=0.3 不穩定，其餘 > 75% |
| 3 狀態估測器 | 是 | **否（多一條噪聲路徑）** | 25~160% |
| **Smith Predictor（= Eq. 17）** | **是** | **是** | **< 2%** |
| Simulink Kalman 濾波器 | 是 | 不同架構（同時估測 ax） | — |

### 8.3 Eq. 13 的物理意義

利用漲落耗散定理：微球的熱力抖動與其可動性成正比。
量測抖動大小，扣除控制器和延遲的放大效應，即可即時反推局部阻力係數。
這讓微球成為一個邊控制邊量測的「局部黏度感測器」。

---

## 9. 檔案清單

| 檔案 | 用途 |
|------|------|
| `verify_eq13.m` | 核心驗證腳本（Smith Predictor） |
| `verify_eq13_estimator.m` | 3 狀態估測器對照實驗 |
| `verify_eq13_report.md` | 本報告 |
| `fig_report_main_v3.png` | 報告用主圖：sig2 vs lc + axm vs lc |
| `fig_report_error.png` | 報告用誤差柱狀圖 |
| `fig_report_convergence.png` | 報告用收斂時間序列圖 |
| `fig_estimator_openloop.png` | 估測器開迴路驗證圖 |
| `fig_estimator_closedloop.png` | 估測器閉迴路驗證圖 |
| `path_control.m` | 參考 — Simulink 模型參數 |
| `motion_3D_1_2_w12_5.slx` | 參考 — 完整 Simulink 模型 |
