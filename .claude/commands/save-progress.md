---
description: Stage changes and commit with descriptive message
---

1. Run `git status` to see all changes
2. Run `git diff --stat` to summarize modifications
3. Stage all relevant changes (exclude .claude/settings.local.json)
4. Create a commit with a descriptive message summarizing the changes
5. Show the commit hash and summary
